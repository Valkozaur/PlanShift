@using PlanShift.Web.ViewModels.Group;
@model GroupListViewModel<GroupBasicInfoViewModel>

@{
    this.ViewData["Title"] = "Change requests";
}

<h2>Shift Changes by group</h2>
<ul class="nav nav-tabs">
    @foreach (var group in this.Model.Groups)
    {
        <li role="presentation" class="nav-link @(Model.ActiveTabGroupId == @group.Id ? "active" : string.Empty)"><a asp-controller="ShiftChange" asp-action="SwitchToTabs" asp-route-activeTabGroupId="@group.Id" asp-route-businessId="@Model.BusinessId">@group.Name</a></li>
    }
</ul>

<vc:shift-changes-per-group group-id="@Model.ActiveTabGroupId"></vc:shift-changes-per-group>

